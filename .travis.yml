language: cpp
compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
    - cmake
    - time
    - libgtest-dev
    - unzip

before_install:
  - bash install-gtest.sh

script:
  - mkdir build && cd build && cmake ../ && make && ./tests