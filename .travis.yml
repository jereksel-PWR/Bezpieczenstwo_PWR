language: cpp
compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
    - cmake
    - time
    - libgtest-dev
    - unzip

before_install:
  - chmod +x install_gtest.sh
  - ./install_gtest.sh

script:
  - mkdir build && cd build && cmake ../ && make && ./tests